import 'package:flutter/material.dart';
import 'package:task/utils/notificationDataList.dart';

Widget buildCardView(context) {
  return Container(
      // child: ListView.builder(
      //   itemCount: notificationDataList.length,

      // )
      );
}
