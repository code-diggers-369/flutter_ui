import 'package:flutter/material.dart';

class BigCard extends StatelessWidget {
  // const BigCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("h"));
  }
}
